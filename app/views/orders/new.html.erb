<h1>Add a new order</h1>

<%= simple_form_for [@seller, @product, @order] do |f| %>
  <%= f.input :size, label: "How many do you want?" %>
  <%= f.button :submit %>
  <%= link_to "Back to all orders", seller_product_orders_path(@seller, @product), class: "btn btn-primary" %>
<% end %>


